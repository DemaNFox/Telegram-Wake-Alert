services:
  telegram-alarm-backend:
    build: .
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./sessions:/app/sessions
      - ./data:/app/data
      - ./firebase-service-account.json:/app/firebase-service-account.json:ro
    restart: unless-stopped
